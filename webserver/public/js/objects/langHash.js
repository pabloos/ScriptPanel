//maybe is better to put this on the server (because we can search the lang not only by the extension)

var langHash = new Map();

langHash.set("sh", "bash");
langHash.set("py", "python");
langHash.set("pl", "perl");
langHash.set("rb", "ruby");